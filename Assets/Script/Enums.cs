using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums : MonoBehaviour
{
    public enum Forme  
    {
        Rond,
        Rectangle,
        Flipper
    }

    public enum Sens
    {
        Right,
        Left
    }
}
